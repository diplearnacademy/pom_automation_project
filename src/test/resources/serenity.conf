serenity {
    take.screenshots = AFTER_EACH_STEP
    project.name = Page Object Model con SerenityBDD
}

environments {
    local {
        webdriver.base.url = "http://local-url.com"
    }
    staging {
        webdriver.base.url = "https://www.automationtesting.co.uk/popups.html"
    }
    production {
        webdriver.base.url = "http://production-url.com"
    }
}

headless.mode = false
webdriver {
    driver = ${driver}
        chrome {
            browserName = "chrome"
            acceptInsecureCerts = true
            "goog:chromeOptions" {
                args = ["remote-allow-origins=*", "test-type", "no-sandbox", "ignore-certificate-errors", "start-maximized",
                        "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", "disable-popup-blocking",
                        "disable-dev-shm-usage", "disable-extensions", "disable-web-security", "disable-translate", "disable-logging"]
        }
        firefox {
            browserName = "firefox"
            acceptInsecureCerts = true
            "moz:firefoxOptions" {
                args = ["-headless"]
            }
        }
        edge {
            browserName = "edge"
            acceptInsecureCerts = true
            "ms:edgeOptions" {
                args = ["--start-maximized"]
            }
        }
    }
}
